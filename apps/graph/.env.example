# Turso Database Configuration
# 
# To use Turso database instead of in-memory database:
# 1. Create a Turso database: turso db create <database-name>
# 2. Get the database URL: turso db show <database-name> --url
# 3. Create an auth token: turso db tokens create <database-name>
# 4. Copy this file to .env and fill in the values below

# Turso database URL (e.g., libsql://your-database.turso.io)
VITE_TURSO_DATABASE_URL=

# Turso authentication token
VITE_TURSO_AUTH_TOKEN=

# If these are not set, the app will use an in-memory SQLite database
# which is useful for local development and testing
